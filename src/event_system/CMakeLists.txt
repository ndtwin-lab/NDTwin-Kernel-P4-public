# src/event_system/CMakeLists.txt
add_library(EventSystemLib STATIC
    EventBus.cpp        # Are these files present and uncommented?
    # EventPayloads.cpp   # Or are there no .cpp files listed here?
    # RequestParser.cpp
)