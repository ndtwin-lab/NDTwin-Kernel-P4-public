# Accepted differences between the OVS baseline and the P4 candidate
#
# Purpose: force every "P4 cannot do this" into writing.
#
# compare_baseline.py fails on any difference not listed here. That is deliberate: it
# means each P4 shortfall must be acknowledged with a reason, instead of quietly
# degrading. When a phase of doc/2026-07-27_p4_bmv2_support_plan.md lands, the corresponding entry
# should be deleted -- the tool reports unused entries so they do not accumulate.
#
# Format (three fields separated by a pipe WITH SPACES around it):
#
#   endpoint | python regex matched against the difference text | why it is accepted
#
# The separator is " | " (whitespace-pipe-whitespace), not a bare pipe, so that regex
# alternation still works: write it without surrounding spaces, e.g. (int|float|number).
#
# endpoint is the response filename without .json (e.g. get_graph_data), or * for any.
# The regex is matched with re.search against the difference description, which looks
# like one of:
#
#   field missing in P4: nodes[].brand_name (is str in OVS)
#   type differs at edges[].flow_set[]: OVS=int P4=<empty list>
#   all_paths_populated: OVS=True P4=False
#   present in OVS but missing from P4
#
# Run compare_baseline.py once with an empty allowlist to see the exact wording before
# writing a rule.
#
# [Co-developed with claude code -- Adam]


# ============================================================================
# Genuine, permanent data-plane differences
# ============================================================================

# The whole point of the switch_kind work: OVS switches report "OVS", bmv2 report "BMv2".
# The field must exist on both sides with the same type; only the value differs, so this
# only fires if something changes the type.
get_graph_data | type differs at nodes\[\]\.brand_name | brand_name is intentionally different per data plane; only a type change would be a fault

# CPU / memory / temperature are SNMP-derived on hardware and dummy values under Mininet
# for both data planes. Magnitudes are meaningless; only coverage is checked as a fact.
get_cpu_utilization | type differs at .* (int|float|number) | Mininet mode returns synthetic utilisation for both data planes
get_memory_utilization | type differs at .* (int|float|number) | as above
get_temperature | type differs | temperature is a per-model SNMP feature; both planes return the unsupported-model string under Mininet


# ============================================================================
# Known gaps, each tied to a phase that removes it
# ----------------------------------------------------------------------------
# DELETE the entry when the phase lands. If compare_baseline.py reports an entry here as
# unused, the gap is closed and the line should go.
# ============================================================================

# --- Phase 5: proxy synthesises sFlow v5 -----------------------------------------
# Until the emitter exists, bmv2 produces no telemetry at all, so every flow-derived
# figure is empty or zero.
#
# [Co-developed with claude code -- Adam]
# ⚠️ These now fire in the OPPOSITE direction (OVS=False P4=True) whenever the OVS baseline was
# captured at a moment with no live rates. That is a bad baseline, not a P4 improvement, and it
# also makes some of these look "unused" when they simply could not occur. A baseline is only
# usable if get_average_link_usage is non-zero and the flows carry rates at capture time --
# check before trusting an L4 result. See doc/2026-07-30_full_test_runbook.md step 1f.
get_detected_flow_data | flows_detected: | Phase 5: bmv2 emits no sFlow yet, so no flows are detected
get_detected_flow_data | any_rate_nonzero: | Phase 5: no telemetry means no rates
get_detected_flow_data | any_packet_rate_nonzero: | Phase 5: no telemetry means no packet rates
get_detected_top_k_flow_data | flows_detected: | Phase 5: as get_detected_flow_data
get_detected_top_k_flow_data | any_rate_nonzero: | Phase 5: as get_detected_flow_data
get_detected_top_k_flow_data | any_packet_rate_nonzero: | Phase 5: as get_detected_flow_data
get_graph_data | any_link_usage_nonzero: | Phase 5: link usage is sFlow-derived
get_graph_data | any_edge_has_flow_set: | Phase 5: flow_set is populated from sFlow samples
get_average_link_usage | avg_link_usage_nonzero: | Phase 5: averaged from sFlow-derived link usage
get_detected_flow_data | list is empty in P4 but populated in OVS: <root list> | Phase 5: no telemetry means the whole flow list is empty
get_detected_top_k_flow_data | list is empty in P4 but populated in OVS: <root list> | Phase 5: as get_detected_flow_data
# [Co-developed with claude code -- Adam]
# Bidirectional, and it has to be: the comparator walks the two edges arrays positionally, but the
# topologies have 288 and 40 edges with different orderings, and which edges carry traffic depends
# on the path it takes. Measured with good baselines on both sides -- OVS had flow_set at indices
# [1, 143, 149, 159, 160], P4 at [25, 27, 33, 35, 39], no overlap and none possible. So one
# direction or the other always fires no matter how healthy the capture is.
#
# This was briefly mistaken for evidence of a bad OVS baseline. That baseline WAS bad (captured
# with avg_link_usage 0.0 and a zero-rate flow), and recapturing it was still the right call, but
# it did not make this difference go away and was never going to.
get_graph_data | list is empty in (P4|OVS) but populated in (OVS|P4): edges\[\]\.flow_set\[\] | the edges arrays are compared positionally, but the topologies differ in size and ordering, so the indices carrying traffic never line up

# --- Phase 6: proxy implements /stats/flow/<dpid> and pushes switch state --------
# The stub returns `flows` as a bare list, while the kernel documents (and OVS mode returns)
# a map of table id -> entries. Every field of every entry therefore reads as "missing in P4",
# which is 150 findings for one fact: the endpoint is not implemented. Ryu's real entries carry
# these via ofctl_rest. Remove all of these when Phase 6 implements the endpoint -- if any
# survive then, the shape is genuinely wrong rather than absent.
# [Co-developed with claude code -- Adam]
# Anchored to the one field that actually differs, measured against the captured baseline rather
# than assumed. Matching is an unanchored re.search, so the previous pattern -- `\[\]\.flows` --
# suppressed *any* missing field anywhere under flows: actions, priority, packet_count, table_id.
# The reason claimed a narrow subset ("match fields like dl_dst and in_port"); the pattern encoded
# none of it, and in_port does not in fact differ at all. The only observed difference is
# `[].flows.<table-id>[].match.dl_dst`, ten times, one per switch.
get_switch_openflow_table_entries | field missing in P4: \[\]\.flows\.[0-9]+\[\]\.match\.dl_dst | ipv4_lpm keys on the destination address only, so OVS's dl_dst match field is genuinely absent -- not a stub. Measured: this is the ONLY field that differs under flows
# [Co-developed with claude code -- Adam]
# REMOVED: `type differs at \[\]\.flows` with the reason "numeric field widths differ between
# P4Runtime reads and Ryu stats". compare_baseline.py reported it as UNUSED against the captured
# baseline -- it suppressed nothing, and no observed difference supports the claim. Being unanchored
# it would have hidden *any* type change under flows, including `actions` becoming a string instead
# of a list. Over-broad and unused is the worst combination: pure risk, no benefit. If a genuine
# width difference ever appears, add it back naming the specific field.
# [Co-developed with claude code -- Adam]
# The flows themselves ARE detected in P4 mode -- the telemetry chain was verified live on
# 2026-07-29 (rx=126 addressed=126, both ICMP directions parsed). Only `path` is empty, for
# the same reason as all_paths_populated above.
# get_path_switch_count returns {"status":"error","message":"Path not found for the given IPs."}
# in P4 mode, so every field of the success shape reads as missing. That is the graph being
# inert (Phase 6), not a broken endpoint -- and reporting an explicit error rather than a
# fabricated count is the behaviour we want. Remove these when Phase 6 lands.
# Both topologies define ecmp_groups, but not on the same switches, so some index is empty on
# one side and populated on the other -- in both directions. A difference in topology *files*,
# not in dataplane behaviour.
get_graph_data | list is empty in (P4|OVS) but populated in (OVS|P4): nodes\[\]\.ecmp_groups\[\] | the two topology files populate ecmp_groups on different switches

# --- Phase 7: real bmv2 process control ------------------------------------------


# ============================================================================
# Examples of what must NOT be added here
# ----------------------------------------------------------------------------
# These would hide real bugs. Left as comments so the intent is unambiguous.
#
#   get_graph_data | all_switches_up:          <- a switch being down is a fault, not a
#                                                 P4 limitation
#   get_graph_data | all_switches_enabled:     <- this is exactly the Phase 6 bug we are
#                                                 hunting; allowlisting it defeats the
#                                                 purpose of the comparison
#   get_graph_data | has_switches:             <- an empty graph is never acceptable
#   * | present in OVS but missing from P4     <- a missing endpoint response means the
#                                                 run did not complete
# ============================================================================
